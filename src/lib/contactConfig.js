// Shared studio contact email — displayed on the site (Contact/Footer) and
// used as the display source for the Contact form's admin notification.
// The actual delivery address for that notification is controlled
// server-side by ADMIN_NOTIFICATION_EMAIL in .env.local (see
// src/app/api/contact/enquiry/route.js) and should be kept in sync with
// this value.
export const STUDIO_EMAIL = 'vedukabydheeraj@gmail.com';
